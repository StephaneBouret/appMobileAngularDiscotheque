import { SurvolDirective } from './survol.directive';

describe('SurvolDirective', () => {
  it('should create an instance', () => {
    const directive = new SurvolDirective();
    expect(directive).toBeTruthy();
  });
});
